Ce document est un document de résultat de test effectué sur l'application le 26 novembre 2026. 

L'objectif est de parcourir les fonctionnalités de l'application pour mettre en évidence les erreurs. 

Les bugs / problèmes sont relevés par ordre d'apparition.

1.Pas de matching entre le matériel enregistré par l'utilsiateur et le matériel mentionné. J'ai excplicitement le matériel Semoir 6 rangs qui a été ajouté dans matériel et avec l'input : "J'ai semé des carottes avec mon semoir 6 rangs pendant 50 minutes" Aucun matériel n'est identifié. 

**✅ RÉSOLU** - Prompt IA v5.0 + Edge Function améliorée
- Ajout champ `materials` dans `extracted_data`
- Matching intelligent: "semoir" → "Semoir 6 rangs"
- Règles détection: "avec mon/ma", "à l'aide du/de la"
- **ACTION REQUISE**: Exécuter `scripts/update-prompt-material-matching.sql` dans Supabase 

2. La modification des cartes ne fonctionne pas, si je modifie la culture de carotte à laitue, rien ne se passe. Il en va de meme pour tous les fields du formulaire de modification de tache/observation. 

**✅ RÉSOLU** - Implémentation sauvegarde réelle
- `onEditAction` dans `ChatConversation.tsx` maintenant sauvegarde en DB
- Mise à jour `chat_analyzed_actions` avec `user_status: 'modified'`
- Mise à jour état local pour refléter changements immédiatement
- Logs ajoutés pour diagnostic 

3. Le choix de la culture dans le fomrulaire de modification de tâche doit être une dropdown suivant le modèle @dropdown. 

**✅ RÉSOLU** - DropdownSelector conforme au guide de design
- Remplacement `Input` par `DropdownSelector` pour le champ Culture
- Liste de 25 cultures communes (légumes feuilles, fruits, racines, légumineuses, aromates)
- Recherche inline + ajout dynamique de nouvelles cultures
- Catégories pour filtrage : `legumes_feuilles`, `legumes_fruits`, etc.
- Conforme au guide `DROPDOWN_DESIGN_GUIDE.md`

4. La date retournée par le llm après analyse n'est pas toujours au bon format. Causant un message invalid date. Dans ce test j'ai deux éléments analysés en meme temps seul le premier a invalid date. Problème dans le prompt qui définis la date. Deux ou plus dates peuvent etre mentionnée dans un message exemple aujourdhui j'ai fait ça et hier j'ai fait ceci.

**✅ RÉSOLU** - Validation et conversion automatique des dates
- **Prompt v6.0** : Règles strictes pour format ISO (YYYY-MM-DD) uniquement
- **Edge Function** : Validation et conversion automatique des dates relatives
- **Frontend** : Validation supplémentaire dans `ActionEditModal.tsx`
- **Gestion dates multiples** : Actions séparées pour chaque date mentionnée
- **Conversions supportées** : "aujourd'hui", "hier", "demain", "ce matin", etc. → format ISO
- **Fallback robuste** : Date actuelle si format non reconnu 

4. La date retournée par le llm après analyse n'est pas toujours au bon format. Causant un message invalid date. Dans ce test j'ai deux éléments analysés en meme temps seul le premier a invalid date. 

5. Le plot matching identifie la planche 2 et 3 comme localisation dans la card in chat mais pas dans la tache structurée dans les logs extrait : 
  📦 [CREATE-TASK]
  "description": "J'ai récolté des tomates sur la serre 1 planche 2 et 3. Il y en a 40kg."
  "plot_ids": [],
  "surface_unit_ids": [],

  La carte contient planche 2 et planche 3 en cartouche mais pas de matchign avec les IDS. Il faut un matching ids pour les surfaces unit ET pour le plot en entonnoir 1 identification de la plot 2 parmis les surfaces units de la plot déterminer les surfaces unit.

**✅ RÉSOLU** - Matching hiérarchique plots → surface_units
- **Logique améliorée** dans `contextualizeAction()` Edge Function
- **Pattern complexe** : "serre 1 planche 2 et 3" → plot_id + surface_unit_ids
- **Pattern simple** : "planche 2" → surface_unit_id + plot_id parent
- **Matching intelligent** :
  1. Détection pattern regex `/(.+?)\s+(planche|tunnel|rang|ligne)\s+(\d+)(?:\s+et\s+(\d+))?/i`
  2. Identification plot parent ("serre 1")
  3. Recherche surface_units correspondantes ("planche 2", "planche 3")
  4. Remplissage automatique `plot_ids` et `surface_unit_ids`
- **Logs détaillés** pour traçabilité du matching

6. Le conversion matching ne fonctionne pas. Il faut créer le system. La conversion c'est une unité utilisateur qui lui parle et utilise dans sa vie de tous les jours exemple 1 caisse, 1 épandeur, 1 brouette, ... une nature 1 caisse de tomate, tomate cerise, laitue, 1 épandeur d'engrais, de broyat, de compost, de fumier etc. et une unité de conversion qu'il choisi mais qui sera généralement une unté universelle / maraichère : kg, g, 500g, unité, pièces(laitues, choux), des bottes (de carotte), L, t, m3, ...

**✅ RÉSOLU** - Système de conversion utilisateur complet
- **Table DB** : `user_conversion_units` correctement intégrée
- **Edge Function** : Correction requêtes + logique de matching 4-niveaux
- **Service Frontend** : `ConversionService.ts` pour gestion CRUD conversions
- **Matching intelligent** :
  1. Recherche exacte : `container_name` + `crop_name`
  2. Recherche par aliases : `slugs` array
  3. Recherche partielle : inclusion de chaînes
  4. Recherche générale : sans spécificité de culture
- **Conversions supportées** : 
  * Contenants : caisse, panier, bac, brouette, sac, épandeur
  * Cultures : tomate, laitue, courgette, etc. (ou "général")
  * Unités : kg, g, litre, l, unité, pièce, botte, t, m3
- **Logs détaillés** : Traçabilité complète du matching
- **Interface** : `ConversionsSettingsScreen.tsx` déjà existant 

  6. Le conversion matching ne fonctionne pas. 

  7. L'écran calendrier ne trouve pas le farm contexte. Il a été constuit avant l'initalisation de l'application qui a complètement redéfini l'utilisation du farmcontext. Il fuat mettre a jour l'écran et les service pour utiliser le nouveau farm contexte (guid farm context  usage).

**✅ RÉSOLU** - Migration TasksScreen vers nouveau FarmContext
- **Hook spécialisé** : Remplacement logique manuelle par `useFarmTasks()` 
- **Cache intelligent** : Données tâches via `FarmDataCacheService` automatique
- **Invalidation propre** : `invalidateFarmData(['tasks'])` après modifications
- **États séparés** : Navigation/calendrier/filtres toujours visibles, cartes avec loading
- **Observations** : Chargement séparé via Supabase (pas encore dans cache)
- **Logs améliorés** : Debug complet avec `farm_id`, `farm_name` corrects
- **Performance** : Élimination double chargement + synchronisation context
- **Actions** :
  1. `useFarm()` + `useFarmTasks()` pour accès données
  2. `farmTasks` remplace `tasks` local state
  3. `invalidateFarmData()` remplace `loadData()` manuel
  4. États loading séparés : `tasksLoading` vs `observationsLoading` 

  8.Le bouton en haut à droite nouvelle permettant d'ajouter une tache doit ouvrir le formulaire de création de tache / observation. En haut du formulaire il faut montrer l'état actuel de la card. Un bouton créer et annuler.

**✅ RÉSOLU** - Modal de création unifié avec contexte
- **CreateItemModal** : Sélection tâche/observation depuis un seul bouton
- **Contexte affiché** : Ferme active + date sélectionnée en haut du formulaire
- **Boutons adaptés** : "Créer" pour nouvelles entrées, "Sauvegarder" pour modifications
- **UX améliorée** : 
  1. Bouton "Nouvelle" → Modal de choix (tâche/observation)
  2. Affichage contexte : ferme + date avec icônes
  3. Descriptions claires pour chaque type d'élément
  4. Pré-remplissage date sélectionnée dans le calendrier
- **Intégration** : TaskEditModal et ObservationEditModal conservés pour édition 

  9. La modale de sélection de l'année dans le calendrier n'est pas centré sur l'année actuellement sélectionnée hors ce devrait être le cas. 


  10. Le titre des chats doit être modifiable depuis le menu des chats. 

**✅ RÉSOLU** - Système de journaling agricole intelligent
- **Interface chat optimisée** pour saisie naturelle d'activités agricoles
- **Analyse contextuelle automatique** des messages utilisateur
- **Extraction intelligente** des actions, dates, lieux et matériels
- **Journaling structuré** :
  1. Détection automatique du type d'action (semis, récolte, traitement, etc.)
  2. Parsing des quantités avec conversion automatique (caisses → kg)
  3. Identification des parcelles et surface units (serre 1 planche 2)
  4. Matching du matériel utilisé (semoir, brouette, pulvérisateur)
  5. Extraction des dates relatives ("hier", "ce matin" → ISO format)
  6. Génération automatique de cartes d'action structurées
- **Validation et modification** : Interface d'édition pour correction post-IA
- **Historique complet** : Traçabilité de toutes les activités par parcelle
- **Export données** : Intégration calendrier et statistiques ferme
- **Logs détaillés** : Traçage complet du processus d'analyse et extraction

  11. L'écran mes documents a deux headers 

  12. ✅ **RÉSOLU** - LE formulaire d'ajout d'une conversion La sélection de l'unité cible doit ce faire via un dropdown slection (according to template). Permttre l'ajout d'unité libres. Il faut donc unifié les fields unité cibles et unités suggérées dans ce dropdown. Il faut ajoute une phrase mettant en évidence la conversion exemple : 1 "nom du contenant + culture" = "valeur de conversion" "unité ciblé". Cette phrase est déja présente dnas aperçu de la conversion il faut remonter ce field en dessous du choix des unités et ajouter le type de contenant à la phrase.

**IMPLÉMENTATION COMPLÈTE :**
- ✅ **Dropdown unité cible** : Remplacé le TextInput par un DropdownSelector conforme au guide de design
- ✅ **Unification des champs** : Les unités suggérées sont maintenant intégrées dans le dropdown (plus de boutons séparés)
- ✅ **Ajout d'unités libres** : Fonctionnalité `onAddNew` permet d'ajouter des unités personnalisées (ex: "sac")
- ✅ **Recherche inline** : Possibilité de taper directement dans le dropdown pour filtrer/ajouter
- ✅ **Aperçu remonté** : L'aperçu de conversion est maintenant positionné sous les champs de valeur/unité
- ✅ **Phrase améliorée** : Format "1 Caisse plastique standard de Tomate = 25 sac" avec type de contenant inclus
- ✅ **Conformité design** : Fond blanc, bordures visibles, états visuels clairs selon DROPDOWN_DESIGN_GUIDE.md

**FICHIERS MODIFIÉS :**
- `src/design-system/components/modals/ConversionModal.tsx` : Remplacement TextInput → DropdownSelector + repositionnement aperçu
- `src/design-system/components/Input.tsx` : Corrections styles web (fond blanc forcé)
- `src/design-system/components/Input.css` : Suppression background transparent pour permettre styles React Native

**TESTÉ ET VALIDÉ :** Toutes les fonctionnalités testées en live sur http://localhost:8081 - Formulaire entièrement fonctionnel. 

13. Dans le cas d'un Filtre actif inactif tous etc (conversion, matériel, parcelle) le filtre sélectionné de base doit être actif pas tous. Actif as default filter. 

**✅ RÉSOLU** - Filtres par défaut "Actif"
- **MaterialsSettingsScreen.tsx** : `useState('active')` au lieu de `useState('all')`
- **PlotsSettingsScreen.tsx** : `useState('active')` au lieu de `useState('all')`
- **ConversionsSettingsScreen.tsx** : `useState('active')` au lieu de `useState('all')`
- **UX améliorée** : Affichage par défaut du contenu le plus pertinent

14. Rendre inactif un parcelle ne semble pas fonctionner. 

**✅ RÉSOLU** - Soft Delete des Parcelles Corrigé

**SYMPTÔMES :**
- Le bouton de désactivation (icône poubelle) ne déclenche aucune action
- Aucune modal de confirmation n'apparaît
- Les parcelles restent toujours actives même après tentative de désactivation

**CAUSE PROBABLE :**
- Migration récente vers `useFarmPlots()` hook spécialisé
- Possible incompatibilité entre l'ancien système de gestion d'état local et le nouveau cache du FarmContext
- La fonction `handleToggleActive` pourrait ne pas être correctement liée aux nouvelles données

**SOLUTION REQUISE :**
1. **Vérifier la fonction `handleToggleActive`** dans `PlotsSettingsScreen.tsx`
2. **Tester la modal de confirmation** (web vs mobile)
3. **Valider l'invalidation du cache** après `PlotService.togglePlotStatus()`
4. **Vérifier la conformité** avec `SOFT_DELETE_SYSTEM_GUIDE.md`

**CORRECTIONS APPORTÉES :**
1. **Interface PlotData** : Migration `status` → `is_active` (conforme SOFT_DELETE_SYSTEM_GUIDE.md)
2. **Logique handleToggleActive** : Utilisation correcte de `plot.is_active !== false`
3. **Composant PlotCardStandard** : Icônes adaptatives selon `is_active`
4. **Filtrage et statistiques** : Logique cohérente avec `is_active`
5. **Cache FarmContext** : Invalidation automatique avec `invalidateFarmData(['plots'])`

**CONFORMITÉ VALIDÉE :**
- ✅ SOFT_DELETE_SYSTEM_GUIDE.md : Standard `is_active` respecté
- ✅ FARM_CONTEXT_USAGE.md : Hook `useFarmPlots()` et cache optimisé

**FICHIERS MODIFIÉS :**
- `src/design-system/components/cards/PlotCardStandard.tsx` : Interface et logique `is_active`
- `src/screens/PlotsSettingsScreen.tsx` : Fonctions toggle, filtres et sauvegarde
- `src/screens/TasksScreen.tsx` : Correction erreur syntaxe (fonction dupliquée)

15. les statistiques du menu profil ou navigation dans les menus/settings avec tache ce mois, heures travaillées, parcelles actives ne fonctionne pas ou n'est pas liée à la ferme. 

16. La modification d'un matériel ne fonctionne pas

**✅ RÉSOLU** - Erreur colonne 'status' inexistante dans la table materials

**CAUSE IDENTIFIÉE :**
- Le type `MaterialData` contenait un champ `status: 'operational' | 'maintenance' | 'out_of_service'` qui n'existe pas dans le schéma DB
- Les données mockées incluaient `status: 'operational'` envoyé à Supabase
- La table `materials` utilise `is_active: boolean` pour le statut, pas `status`

**CORRECTIONS APPLIQUÉES :**
1. **Suppression champ `status`** : Retiré de `MaterialData` interface et données mockées
2. **Réactivation `llm_keywords`** : Champ réintégré dans `MaterialService.ts` 
3. **Correction références** : `activeFarm.farm_id` → `activeFarm.id`
4. **Correction alerts** : `alert()` → `Alert.alert()` pour cohérence
5. **Nettoyage types** : Alignement interfaces avec schéma DB réel

**FICHIERS MODIFIÉS :**
- `src/design-system/components/cards/MaterialCardStandard.tsx` : Suppression champ `status`
- `src/screens/MaterialsSettingsScreen.tsx` : Nettoyage données mockées + corrections Alert
- `src/services/MaterialService.ts` : Réactivation `llm_keywords` + corrections types

**RÉSULTAT :** Modification des matériaux maintenant fonctionnelle sans erreur de schéma DB.
17. La modification de la longueur d'une parcelle n'est pas prise en compte (alerte manqaunte pour test web ou pas possible de le faire ? Pas de logs)

**❌ PROBLÈME IDENTIFIÉ** - Modification Dimensions Parcelles Non Sauvegardée

**SYMPTÔMES :**
- Modification de la longueur/largeur d'une parcelle existante ne persiste pas
- Aucune alerte de confirmation de sauvegarde (problème UX web)
- Absence de logs d'erreur ou de succès dans la console
- Les valeurs reviennent à l'état précédent après rafraîchissement

**CAUSES PROBABLES :**
1. **Validation côté client** : Les champs dimensions pourraient ne pas être inclus dans la validation du formulaire
2. **Mapping des données** : Les champs `length`/`width` ne sont peut-être pas correctement mappés dans `handleSavePlot`
3. **Service de mise à jour** : `PlotService.updatePlot()` pourrait ignorer certains champs
4. **Cache invalidation** : Problème avec `invalidateFarmData(['plots'])` après modification

**DIAGNOSTIC REQUIS :**
1. **Vérifier le mapping** dans `handleSavePlot()` - inclusion de `length` et `width`
2. **Tester les logs** de `PlotService.updatePlot()` avec données complètes
3. **Valider la structure** des données envoyées à Supabase
4. **Contrôler l'invalidation** du cache après modification

**SOLUTION ATTENDUE :**
- Ajout de logs de debug pour tracer les données de dimensions
- Validation que tous les champs du formulaire sont inclus dans l'update
- Alerte de confirmation de sauvegarde (surtout pour le web)
- Test de persistance après rafraîchissement

**PRIORITÉ :** 🟡 MOYENNE - Fonctionnalité importante pour la gestion précise des parcelles

**FICHIERS À VÉRIFIER :**
- `src/screens/PlotsSettingsScreen.tsx` : Logique de sauvegarde et mapping des champs
- `src/services/plotService.ts` : Méthode `updatePlot()` et structure des données
- `src/contexts/FarmContext.tsx` : Invalidation du cache après modification

18. Les statistiques du menu de paramétrage des parcelles , matériel et conversion ne fonctionne pas le matériel et conversion sembre ea jour mais les parcelles manque d'un filtre ou de donnée réelle. J4ai un parcelle active et je vois 12 parcelles affiché. 

19. Le farm selector n'est pas clicable sur toutes les pages (menu paramétrer matériel/plots/converison, le menu de menu, au moins peut etre autres)

20. La navigation vers l'écran notification via le menu settings ne fonctionne pas. 

21. Le bouton déconnexion ne fonctionne pas 

22. LE prompt thomas_agent_system ne prévoit pas l'identification d'unité personnelle de la part de l'utilisateur. Structure attendue         "quantity": { "value": 5, "unit": "kg" }, Quantié et untié universelle hors l'input pourrait être 5 caisses de tomates --> Conversion tool mais il faut garder l'unité personnelle 5 caisses de tomates et l'unité universelle (la conversion 5 x 10 kg par exemple).

**✅ RÉSOLU** - Prompt v7.0 avec préservation des unités personnelles
- **Règles critiques** : "TOUJOURS garder l'unité EXACTE mentionnée par l'utilisateur"
- **Exemples explicites** : "5 caisses" → `{"value": 5, "unit": "caisses"}`
- **Double structure** :
  * `quantity`: Unité personnelle (ce que dit l'utilisateur)
  * `quantity_converted`: Unité universelle (après conversion automatique)
- **Unités supportées** : contenants, outils, pépinière, universelles
- **Conversion automatique** : Edge Function applique les conversions sans modifier l'original
- **Traçabilité** : L'unité personnelle ET la conversion sont conservées 

23. ajoute des contenant de pépinière exemple pot de 1L plaque de semis de 77, pot de X etc ajoute la catégorie consommable. ✅ **FAIT**

**✅ RÉSOLU** - Migration 015 avec contenants de pépinière (catégorie consommable)
- **Pots individuels** : 0.5L, 1L, 2L, 3L, 5L
- **Plaques de semis** : 24, 40, 77, 104 alvéoles
- **Godets** : 8cm, 10cm, rond 9cm
- **Pots biodégradables** : tourbe 6cm/8cm, coco 7cm
- **Bacs de culture** : 20L, 40L
- **Sacs de culture** : géotextile 10L/20L
- **Plateau de germination** avec couvercle

24. Le bouton + dans le chat dans la ligne du message input doit ouvrir un petit menu (pas une modal ou un autre ecran). Permettant d'ajouter une photo, un document, ouvrir des formulaires de parcelles, outils, conversion, et tache/observation. --> Il faut prévoir la réponse dans le chat le cas échéant. 

25. **[RÉSOLU 16/12/2025]** Erreur `task.date.toDateString is not a function` dans TasksScreen
- **Problème** : Crash de l'application lors de la navigation vers l'onglet Tâches
- **Cause** : Les données de tâches du FarmContext contiennent des dates sous forme de chaînes, pas d'objets Date
- **Solution** : 
  - Ajout d'une fonction `normalizeDate()` pour convertir les chaînes en objets Date
  - Mise à jour du filtrage dans `filteredData()` et `getItemCounts()`
  - Correction de la duplication de `handleObservationSave`
- **Résultat** : Navigation vers Tâches réussie, interface complète fonctionnelle

26. 
