[build]
  command   = "npm run build:web"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: toutes les routes → index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
